// Напишіть функцію isPowOfTwo, яка за допомогою побітових операцій визначатиме чи є
// довільне ціле число ступенем двійки. Наприклад, 16 є ступенем числа 2, а 14 – ні.

function isPowOfTwo(num) {
    if (num === 0) {
        return false;
    }

    return (num & (num - 1)) === 0;
}